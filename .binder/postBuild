#!/bin/bash
set -e

echo "🔧 PostBuild: Configuring environment..."

# Ensure correct permissions
chmod +x /usr/local/bin/start-ldaca-services.sh 2>/dev/null || true
chmod +x /usr/local/bin/start.sh 2>/dev/null || true

echo "✅ PostBuild: Configuration complete"
