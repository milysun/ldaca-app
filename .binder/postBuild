#!/bin/bash

echo "🚀 BinderHub PostBuild: LDaCA Setup"

# Create work directory
mkdir -p ${HOME}/work

# Make start script executable  
if [ -f "/usr/local/bin/start-ldaca.sh" ]; then
    sudo chmod +x /usr/local/bin/start-ldaca.sh
fi

echo "✅ PostBuild complete"
