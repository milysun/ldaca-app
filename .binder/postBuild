#!/bin/bash
set -e

echo "� PostBuild: Setting up LDaCA for BinderHub"

# Start the LDaCA services in background
echo "🔄 Starting LDaCA services launcher..."
/usr/local/bin/start-ldaca-services.sh &

echo "✅ PostBuild: LDaCA service launcher started in background"
echo "� Services will be available in ~15 seconds after Jupyter starts"
echo "🌐 Check /tmp/ldaca-startup.log for service status"
