#!/bin/bash
# JupyterHub single-user server wrapper for BinderHub compatibility

echo "🔧 Starting JupyterHub single-user server with LDaCA..."

# Ensure we have the right environment
export USER=${USER:-jovyan}
export HOME=${HOME:-/home/jovyan}

# Start our notebook server which includes LDaCA
exec /usr/local/bin/start-notebook.sh "$@"
